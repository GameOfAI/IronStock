// Package auth handles user authentication: password hashing (Argon2id),
// TOTP (RFC 6238) enrollment and verification, session tokens (JWT),
// and RBAC role checks. See docs/adr/0002-security-model.md.
package auth
