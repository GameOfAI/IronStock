// Package httpapi contains the REST HTTP layer: router, middleware
// (logging, auth, RBAC, rate limit), and handler functions. Named
// httpapi (not http) to avoid collision with the standard net/http package.
package httpapi
