// Package audit writes audit log entries for every mutation (auth events,
// inventory CRUD, permission changes). Audit entries are stored server-side
// in plaintext for compliance and readability.
package audit
